package ${classPath}.module.${moduleName}.bean.request;

/**
 * Created by `wall.e`(https://github.com/slashchenxiaojun/wall.e)
 */
public enum ${enumClassName} {
@ for ( v in enumValues ) {
@   print("  " + v);
@   if ( vLP.last != true ) { print(",\r\n"); } else { print("\r\n"); }
@ }
}